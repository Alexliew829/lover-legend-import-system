Lover Legend Import Cost & Inventory System V10.7 Stable

Apps Script / Code.gs：需要更新（版本握手 V10.7）。
appsscript.json：不需要更新。
Google Sheet Schema：不需要升级。

部署顺序：
1. 替换 Import System Apps Script 的 V10.7 Code.gs 并 Save。
2. Deploy New Version。
3. 上传 V10.7 Frontend 到 GitHub Pages。
4. Sales System 必须已部署 V25.6 P4 Inventory Feed，且 getSalesInventoryFeed 可读取。

V8.7 Sales Feed 为独立只读请求，不修改原本 Import cloud sync / Pull / Push。

V10.7 note: remark visibility is UI-only. Code.gs business logic is unchanged; however REQUIRED_CLIENT_VERSION / version handshake is updated to 9.1, so deploy the included Code.gs together with Frontend.
