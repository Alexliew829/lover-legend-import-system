Lover Legend Import Cost & Inventory System V11.5 Stable

Apps Script / Code.gs：需要更新并重新部署。
appsscript.json：不需要修改（没有新增权限）。

部署步骤：
1. 用本包 Apps Script/Code.gs 替换现有 Code.gs 并保存。
2. Deploy > Manage deployments > Edit > New version > Deploy。
3. 上传 Frontend 全部文件到现有网站位置。
4. iPhone/PWA 若仍显示旧缓存，重新打开网页并完成一次强制刷新。

V11.5 核心修复：Sales 库存只有在 Products、Imports、Batches、stockAdjustments / History 与精确 Sales Key 全部写入并读回一致后才成功；任何失败自动回滚并保留 Pending。
