V2.5.5
- 修复旧进口记录把剩余数量从 0 改回 1 后，平均成本仍为 RM0.00。
- 当旧记录缺少 unitCost / batchTotal 时，系统会从原批次费用按原始分摊公式重建每棵成本。
- 首页库存数量、平均成本和库存成本总值会依据所有剩余进口记录重新计算。
- 不改变现有排版、进口历史及其他计算逻辑。

V2.5.4
- 修复旧进口明细存在，但产品主档库存未写入，导致首页漏掉产品及库存少算。
- 首页与库存管理会依据进口明细自动校正产品库存。
- 同时按各批次当前剩余数量 × 单位成本重新计算加权平均成本，修复库存成本总值错误。
- 兼容旧资料：若 Imports 缺少记录，会从 Batch.items 补回计算，不会重复计算同一记录。

V2.5.2
- 修复删除较新进口编号后，旧批次剩余库存没有重新计入首页的问题。
- 删除批次后，相关产品改为根据所有剩余进口明细重建库存、平均成本及库存成本总值。
- Imports 工作表新增“原进口数量”和“当前剩余数量”，避免同步后丢失库存调整资料。
- Google Apps Script 日常同步不再每次格式化及自动调整整张 Sheet 栏宽，大幅减少同步等待。
- 不改变现有页面排版及进口成本计算公式。

V2.5.1
- 修复删除较新的进口编号后，旧批次仍有库存但首页不显示产品的问题。
- 首页与库存导出改为以实际库存数量 > 0 为显示依据，不再被旧的 inventoryArchived 标记错误隐藏。
- 删除批次时按产品编号或“产品名称 + 类别”双重识别，并重新恢复旧批次的最后进口日期。
- 不改变排版、成本公式、进口历史及其他运算逻辑。

Lover Legend Import Cost & Inventory System V2.5

- 放大产品进口页的进口编号输入框，长编号可完整查看。
- 当前库存数量允许增加或减少，支持销售扣减、顾客退货及盘点修正。
- 原进口数量保持只读且不改变。
- 库存数量调整不会改变原成本、Average Cost、海外运费比例或整批历史总成本。
- Dashboard 产品种类按盆栽、花盆、周边产品分类显示。
- 新增只读 Import History / 进口历史页面。
- Apps Script 无修改，不需要重新部署。

V2.2
- 首页进口编号可点击复制，并显示“已复制”。
- 放大、加宽 Header，新增红色粗体 Lover Legend Gardening。
- 保留 V2.1 Excel 与 V2.0 同步逻辑。

V2.2
- Excel 的 Inventory 工作表只导出当前库存数量大于 0 且未被移除的产品。
- 删除批次、测试后留下的零库存或已移除产品不再导出。
- Imports 与 Batches 工作表继续保留完整正式进口历史。

V2.0.1
- 首页库存卡片的产品名称旁显示最近一次对应的进口编号。
- 保留进口编号搜索，并更新前端缓存版本，确保部署后立即取得新页面。

正式版 V2.0

同步机制重构：
- 所有产品、进口、库存、批次及汇率设置统一进入同步队列。
- 本地未同步状态会持久保存，断网、关闭页面或重新打开后仍会继续同步。
- 新增进口不会在首次拉取云端资料时被旧资料覆盖。
- 云端版本冲突时自动合并较新记录，再安全提交，不再强制覆盖。
- 连续保存产品、进口明细及批次只合并为一次同步。
- 同步进行中若再次修改，会保留第二次修改并只追加一次必要同步。
- 移除同步完成后的自动 Refresh，直接更新当前页面资料。
- Service Worker 改为更稳定的缓存策略，页面启动与重复打开更快。

保留：
- V1.9 原有页面排版。
- 原有进口成本、库存、日期及编号计算逻辑。

V2.5 Inventory Zero Fix
- 编辑现有进口编号时，当前剩余数量允许设为 0。
- 库存为 0 后，首页库存管理不再显示该产品，但进口历史继续保留。
- 当前剩余数量可在 0 至原进口数量之间增加或减少，用于销售、退货及盘点修正。
- 原进口数量、Average Cost、海外运费比例及原批次成本保持不变。
- Apps Script 无修改，不需要重新部署。

V2.5 Cumulative Inventory Limit Fix
- 编辑进口编号时，数量代表同一产品的当前总库存。
- 可编辑范围：0 至同一产品累计原进口数量。
- 例：第一批进口5棵，第二批再进口3棵，当前库存允许0至8，不允许9。
- 销售、退货、盘点只改变当前库存。
- 每张进口编号的原进口数量、原成本、Average Cost及海外运费比例保持不变。
- Code.gs 未修改，不需要重新部署 Apps Script。
V2.5 Batch Remaining Quantity Fix
- 编辑 A001 只显示并调整 A001 自己的剩余数量，不会显示同产品累计库存。
- A001 原进口 5、A002 原进口 3：编辑 A001 显示 5，编辑 A002 显示 3。
- 每个进口编号可编辑范围为 0 至该批次原进口数量。
- 产品总库存按该批次新旧剩余数量的差额自动加减。
- 原进口历史、Average Cost、海外运费比例及历史成本不变。
- Code.gs 未修改，不需要重新部署 Apps Script。

V2.5 Multiple Batch Display Fix
- 同一产品再次进口时始终生成新的独立进口编号。
- 首页同一产品显示所有仍有库存的进口编号及各批次当前剩余数量。
- 进口历史显示同产品其他进口编号、原进口、当前剩余与同产品总库存提示。
- 产品当前库存仍为所有批次剩余数量合计。
- Code.gs 未修改，不需要重新部署 Apps Script。

V2.5.4
- 修复旧进口记录 unitCost 为 0 时，首页平均成本显示 RM 0.00。
- 优先用 batchTotal / 原进口数量恢复每棵成本；必要时再由 purchaseRM 与运费比例恢复。
- 编辑进口剩余数量后，不再只改库存差额，而是根据全部剩余进口记录重算库存与平均成本。
- 修复忠盛过桥连体4000恢复库存后，库存数量正确但库存成本总值仍错误的问题。
